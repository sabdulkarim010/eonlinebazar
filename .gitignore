# Node modules (এটা খুব জরুরি, কারণ এটি বিশাল বড় ফাইল)
node_modules

# Environment variables (সবচেয়ে গুরুত্বপূর্ণ)
.env
!.env.example

# Baileys WhatsApp session (scan-to-connect auth state)
.wa-auth

# Logs and system files
*.log
.DS_Store

# Generated Prisma Client (build output — recreate with `npx prisma generate`)
/generated/prisma











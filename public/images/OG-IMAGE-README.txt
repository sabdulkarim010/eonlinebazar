Replace public/images/og-default.jpg with your branded Open Graph image.

Recommended size: 1200 x 630 pixels (PNG or JPG).
Used as the fallback when a product has no image and for general storefront pages.

Set DEFAULT_OG_IMAGE_URL in .env if the image is hosted elsewhere.
